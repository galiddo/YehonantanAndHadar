{
  "name": "yehonantan-and-hadar",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/fonts dist/assets && cp index.html dist/ && cp fonts/*.woff fonts/*.ttf dist/fonts/ && cp assets/* dist/assets/"
  }
}
