{
  "name": "yehonantan-and-hadar",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build step'",
    "deploy": "wrangler pages deploy . --project-name=yehonantan-and-hadar"
  },
  "devDependencies": {
    "wrangler": "^3"
  }
}
